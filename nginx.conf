server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;
  
  # Serve static assets directly, hard 404 if missing
  location /assets/ {
    try_files $uri =404;
  }

  # SPA fallback for everything else
  location / {
    try_files $uri $uri/ /index.html;
  }
}
